<main class="container">
  <%= link_to sanitize("&larr; Back to quotes"), quote_path(@quote) %>

  <div class="header">
    <h1><%= @quote.name %></h1>
  </div>
<%= turbo_frame_tag @quote do %>
  <%= render 'form', quote: @quote %>
<% end %>
</main>
