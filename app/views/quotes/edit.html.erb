<main class="container">
  <%= link_to sanitize("&larr; Back to quotes"), quote_path(@quote) %>

  <div class="header">
    <h1><%= @quote.name %></h1>
  </div>

  <%= render 'form', quote: @quote %>
</main>
